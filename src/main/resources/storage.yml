# Where user data will be stored (Options: json, mysql, postgres)
type: json

storage:
  host: localhost
  # Mysql default port: 3306
  # Postgres default port: 5432
  port: 3306
  database: dbName
  schema: schemaName
  user: user
  password: pass